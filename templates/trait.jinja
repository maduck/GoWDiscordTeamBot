<T>**{{ trait.name }}**</T>
{{ trait.description }}

<T>**{{ trait.troops_title }}**</T>
{% for troop in trait.troops %}{{ troop.name }} `#{{ troop.id }}`{% if not loop.last %}, {% endif %}{% else %}-{% endfor %}